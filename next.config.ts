import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // i18n: {
  //   defaultLocale: 'tr',
  //   locales: ['tr', 'en'],
  // }, // App Router'da desteklenmiyor, src/lib/i18n.ts dosyasında yapılandırıldı
};

export default nextConfig;
